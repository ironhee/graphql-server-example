import { expect } from 'chai';
import {
  getResource,
  getResources,
  createResource,
  removeResource,
  updateResource,
  getOffsetsFromConnectionArgs,
} from './rethinkdb';


describe('rethinkdb', () => {
  it('#getResource', () => {

  });
});
